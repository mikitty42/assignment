<%= form_with(model: @post,local: true,data: { turbo: false } ) do |form| %>
    <%= form.hidden_field :content %>
    
    <p>つぶやき:<%= @post.content %></p>
    
    
    <%= form.submit '登録する' %>
    <%= form.submit '戻る',name: 'back' %>
<% end %>
